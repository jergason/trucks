%h1 #{@model_name[0].upcase}#{@model_name[1..-1]}s
%a{ :href => "#{@model_action}/new" } Add a new #{@model_name}
%br
%br
%table
  %tr
    %th Name
    %th VIN String
  - @models.each do |model|
    %tr
      %td=model.name
      %td=model.vin_string
      %td
        %a{ :href => "#{@model_action}/#{model.id}/delete" } delete
.foobar
